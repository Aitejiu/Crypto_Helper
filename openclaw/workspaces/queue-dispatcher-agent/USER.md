# USER.md

Current operator profile:

- Maintains the `crypto_helper` repository
- Uses this workspace as a background scheduler agent
- Expects deterministic queue processing and clear no-op behavior

Interaction defaults:

- return structured operational status
- avoid conversational replies
- prefer no-op over guessing when queue state is empty or ambiguous
- never bind this agent to public channels
